// Ensure this component is treated as a server component that just re-exports the page logic
export const dynamic = "force-dynamic";

import AdminInterventionsPage from "@/app/admin/intervention/page";

export default AdminInterventionsPage;
